[build]
  functions = "netlify/functions"
  publish = "."
  environment = { SECRETS_SCAN_OMIT_PATHS = "studio/", SECRETS_SCAN_OMIT_KEYS = "SANITY_PROJECT_ID,SANITY_DATASET" }

[functions]
  node_bundler = "esbuild"
